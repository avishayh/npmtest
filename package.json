{
  "name": "npmtest",
  "version": "1.0.5-alpha.8",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "version:alpha": "npm version prerelease --preid=alpha --no-git-tag-version",
    "publish:alpha": "npm run version:alpha && npm publish --tag alpha",
    "publish:alpha:provenance": "npm run version:alpha && npm publish --provenance --tag alpha",
    "setup:provenance": "./setup-local-provenance.sh",
    "publish:alpha:local-provenance": "source .env.provenance 2>/dev/null || true && npm run version:alpha && npm publish --provenance --tag alpha",
    "publish:alpha:act": "./act-publish.sh alpha prerelease",
    "publish:latest:act": "./act-publish.sh latest patch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "loaddash": "^0.0.1-security",
    "lodash": "^4.17.19"
  }
}
